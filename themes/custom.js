import { createMuiTheme } from '@material-ui/core/styles';

// Use this to override the material UI styles
export const overridings = {
  name: 'Custom Theme',
};

export default createMuiTheme(overridings);
